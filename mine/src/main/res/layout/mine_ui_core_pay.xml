<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/base_bg"
    android:orientation="vertical" >

    <EditText
        android:id="@+id/alipayName"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:layout_marginTop="10dp"
        android:background="#ffffff"
        android:hint="请填写你的支付宝真实姓名"
        android:padding="10dp"
        android:textSize="16sp" />

    <TextView style="@style/base_line_vertical1px" />

    <EditText
        android:id="@+id/alipayAccount"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:background="#ffffff"
        android:hint="请填写你的支付宝账户"
        android:padding="10dp"
        android:textSize="16sp" />

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal" >

        <TextView
            android:id="@+id/msg"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginLeft="10dp"
            android:layout_marginTop="10dp"
            android:text="你设置的支付宝账号和姓名不匹配将不能报销"
            android:textColor="#666666"
            android:textSize="11sp" />

        <TextView
            android:id="@+id/phone"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginLeft="10dp"
            android:layout_marginTop="10dp"
            android:clickable="true"
            android:onClick="goPhone"
            android:text="023XXXXXXXX"
            android:textColor="#E84C3D"
            android:textSize="11sp"
            android:visibility="gone" />
    </LinearLayout>

</LinearLayout>